import { Box, Container } from '@mui/material';
import React from 'react';

export const Skills = () => {
    return (
        <Box
            sx={{
                height: '100vh',
                position: 'absolute',
                overflow: 'hidden',
                top: '100vh',
                width: '100%',
                background: 'red',
            }}>
            <Container>

            </Container>

        </Box>
    );
};

